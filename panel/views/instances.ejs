<%- include('components/template') %>
<% /* SSB0aGluayBJJ3ZlIGZvdW5kIG15IHZvaWNlCkknbSBhIGxlZWNoIHN1Y2tpbmcgYmxvb2QgYmFncwpUYXN0ZSBkZWZlYXQsIGl0J3MgYSBzYW5kYmFnClNhbGluZSBTb2x1dGlvbg== */ %>
<main id="content">
  <div class="bg-transparent">
    <div class="flex items-center justify-between px-4 sm:px-8 pt-4 flex-wrap gap-4">

  <!-- Left Side -->
  <h1 class="text-base font-medium text-white">
    <%= req.translations.instances %>
  </h1>

  <% if (req.user.admin === true) { %>
  <!-- Right Side -->
  <label for="instance-toggle" class="flex items-center cursor-pointer select-none">
  <div class="relative w-11 h-6">

    <input type="checkbox"
           id="instance-toggle"
           class="peer sr-only"
           <%= req.query.see === 'other' ? 'checked' : '' %>>

    <!-- Track -->
    <div class="w-full h-full rounded-full
                bg-white/10
                peer-checked:bg-blue-600
                transition-colors duration-300">
    </div>

    <!-- Dot -->
    <div class="absolute top-0 left-0
                w-6 h-6 rounded-full bg-white shadow-md
                transition-transform duration-300
                peer-checked:translate-x-5">
    </div>

  </div>

  <span class="ml-3 text-sm font-medium text-white">
    Show All
  </span>
</label>
  <% } %>

</div>
  </div>
  <% if (req.query.err == "NOTACTIVEYET") { %>
    <div class="rounded-xl bg-amber-800/10 px-4 py-6 m-4 sm:m-7">
      <div class="flex">
          <div class="flex-shrink-0 ml-1.5">
              <svg class="animate-spin mt-2 h-5 w-5 text-amber-400" xmlns="http://www.w3.org/2000/svg"
                  fill="none" viewBox="0 0 24 24">
                  <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4">
                  </circle>
                  <path class="opacity-75" fill="currentColor"
                      d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z">
                  </path>
              </svg>
          </div>
          <div class="ml-5">
              <h3 class="text-sm font-medium text-amber-400">Awaiting Installation</h3>
              <div class="text-sm text-amber-400/50">
                  <p>The instance either isn't installed yet, or is in a failed state.</p>
              </div>
          </div>
      </div>
  </div>
    <% } %>	  
  <div class="px-4 sm:px-6 lg:px-8 mt-8">
    <% if (instances.length == 0) { %>
      <!-- No instances -->
      <div id="noInstancesContainer" class="text-center mt-32 sm:mt-64 px-4">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"
          class="mx-auto h-12 w-12 text-neutral-500">
          <path fill-rule="evenodd"
            d="M11.622 1.602a.75.75 0 0 1 .756 0l2.25 1.313a.75.75 0 0 1-.756 1.295L12 3.118 10.128 4.21a.75.75 0 1 1-.756-1.295l2.25-1.313ZM5.898 5.81a.75.75 0 0 1-.27 1.025l-1.14.665 1.14.665a.75.75 0 1 1-.756 1.295L3.75 8.806v.944a.75.75 0 0 1-1.5 0V7.5a.75.75 0 0 1 .372-.648l2.25-1.312a.75.75 0 0 1 1.026.27Zm12.204 0a.75.75 0 0 1 1.026-.27l2.25 1.312a.75.75 0 0 1 .372.648v2.25a.75.75 0 0 1-1.5 0v-.944l-1.122.654a.75.75 0 1 1-.756-1.295l1.14-.665-1.14-.665a.75.75 0 0 1-.27-1.025Zm-9 5.25a.75.75 0 0 1 1.026-.27L12 11.882l1.872-1.092a.75.75 0 1 1 .756 1.295l-1.878 1.096V15a.75.75 0 0 1-1.5 0v-1.82l-1.878-1.095a.75.75 0 0 1-.27-1.025ZM3 13.5a.75.75 0 0 1 .75.75v1.82l1.878 1.095a.75.75 0 1 1-.756 1.295l-2.25-1.312a.75.75 0 0 1-.372-.648v-2.25A.75.75 0 0 1 3 13.5Zm18 0a.75.75 0 0 1 .75.75v2.25a.75.75 0 0 1-.372.648l-2.25 1.312a.75.75 0 1 1-.756-1.295l1.878-1.096V14.25a.75.75 0 0 1 .75-.75Zm-9 5.25a.75.75 0 0 1 .75.75v.944l1.122-.654a.75.75 0 1 1 .756 1.295l-2.25 1.313a.75.75 0 0 1-.756 0l-2.25-1.313a.75.75 0 1 1 .756-1.295l1.122.654V19.5a.75.75 0 0 1 .75-.75Z"
            clip-rule="evenodd" />
        </svg>
        <h3 class="mt-4 text-md font-medium text-white"><%= req.translations.noInstances %></h3>
        <p class="mt-1 text-sm text-neutral-400"><%= req.translations.noInstancesText %></p>
      </div>
    <% } else { %>
      <!-- Mobile Card View / Desktop Table -->
      <div class="instances-container">
        <!-- Desktop Table Header (Hidden on Mobile) -->
        <div class="hidden md:grid grid-cols-5 gap-4 px-6 py-3 bg-white/5 rounded-t-lg border-b border-white/10">
          <div class="text-sm font-semibold text-white">Instance</div>
          <div class="text-sm font-semibold text-white">Status</div>
          <div class="text-sm font-semibold text-white">RAM Usage</div>
          <div class="text-sm font-semibold text-white">CPU Usage</div>
          <div class="text-sm font-semibold text-white">Disk Usage</div>
        </div>
        
        <!-- Cards Container -->
        <div class="grid grid-cols-1 md:grid-cols-1 gap-4 md:gap-0">
          <% instances.forEach(function (instance) { %>
            <!-- Mobile Card / Desktop Row -->
            <div 
              onclick="window.location.href = '../instance/<%= instance.Id %>';"
              class="instance-card md:contents cursor-pointer group"
              data-instance-id="<%= instance.Id %>"
            >
              <!-- Mobile Card Layout -->
              <div class="md:hidden bg-white/5 rounded-xl p-4 border border-white/10 hover:bg-white/[0.08] transition-all duration-300 hover:scale-[1.02] hover:shadow-lg hover:shadow-black/20">
                <!-- Card Header -->
                <div class="flex items-center justify-between mb-4">
                  <div class="flex items-center gap-3">
                    <div class="rounded-xl bg-white/10 p-2.5 group-hover:bg-white/20 transition-colors">
                      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-white">
                        <path d="M12.378 1.602a.75.75 0 0 0-.756 0L3 6.632l9 5.25 9-5.25-8.622-5.03ZM21.75 7.93l-9 5.25v9l8.628-5.032a.75.75 0 0 0 .372-.648V7.93ZM11.25 22.18v-9l-9-5.25v8.57a.75.75 0 0 0 .372.648l8.628 5.033Z" />
                      </svg>
                    </div>
                    <div>
                      <h3 class="font-medium text-white text-sm"><%= instance.Name %></h3>
                      <span class="text-xs text-neutral-400">Instance</span>
                    </div>
                  </div>
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-neutral-500 group-hover:text-white transition-colors">
                    <path fill-rule="evenodd" d="M16.28 11.47a.75.75 0 0 1 0 1.06l-7.5 7.5a.75.75 0 0 1-1.06-1.06L14.69 12 7.72 5.03a.75.75 0 0 1 1.06-1.06l7.5 7.5Z" clip-rule="evenodd" />
                  </svg>
                </div>
                
                <!-- Card Stats Grid -->
                <div class="grid grid-cols-4 gap-3">
                  <!-- Status -->
                  <div class="bg-white/5 rounded-lg p-3 border border-white/5">
                    <div class="text-xs text-neutral-400 mb-1">Status</div>
                    <span data-status-bg class="inline-flex items-center bg-neutral-600/20 text-neutral-400 px-2 py-1 rounded-full text-xs font-medium">
                      <span data-status>
                        <% if (instance.InternalState) { %>
                          <%= instance.InternalState %>
                        <% } else { %>
                          <svg class="animate-spin h-3 w-3 text-white mr-1 inline-flex" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                          </svg>
                          <%= req.translations.loading %>
                        <% } %>
                      </span>
                    </span>
                  </div>
                  
                  <!-- RAM -->
                  <div class="bg-white/5 rounded-lg p-3 border border-white/5">
                    <div class="text-xs text-neutral-400 mb-1">RAM Usage</div>
                    <span data-ram-usage class="text-sm font-medium text-white">
                      <svg class="animate-spin h-3 w-3 text-white mr-1 inline-flex" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                      </svg>
                      <%= req.translations.loading %>
                    </span>
                    <div class="h-1.5 bg-white/10 rounded-full mt-2 overflow-hidden">
                      <div data-ram-bar class="h-full bg-emerald-500 transition-all duration-300" style="width: 0%"></div>
                    </div>
                  </div>
                  
                  <!-- CPU -->
                  <div class="bg-white/5 rounded-lg p-3 border border-white/5">
                    <div class="text-xs text-neutral-400 mb-1">CPU Usage</div>
                    <span data-cpu-usage class="text-sm font-medium text-white">
                      <svg class="animate-spin h-3 w-3 text-white mr-1 inline-flex" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                      </svg>
                      <%= req.translations.loading %>
                    </span>
                    <div class="h-1.5 bg-white/10 rounded-full mt-2 overflow-hidden">
                      <div data-cpu-bar class="h-full bg-emerald-500 transition-all duration-300" style="width: 0%"></div>
                    </div>
                  </div>

                  <!-- Disk -->
                  <div class="bg-white/5 rounded-lg p-3 border border-white/5">
                    <div class="text-xs text-neutral-400 mb-1">Disk Usage</div>
                    <span data-disk-usage class="text-sm font-medium text-white">
                      <svg class="animate-spin h-3 w-3 text-white mr-1 inline-flex" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                      </svg>
                      <%= req.translations.loading %>
                    </span>
                    <div class="h-1.5 bg-white/10 rounded-full mt-2 overflow-hidden">
                      <div data-disk-bar class="h-full bg-emerald-500 transition-all duration-300" style="width: 0%"></div>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Desktop Table Row (Hidden on Mobile) -->
              <div class="hidden md:grid grid-cols-5 gap-4 px-6 py-4 bg-white/5 border-b border-white/5 hover:bg-white/[0.05] transition-colors items-center">
                <!-- Instance Name -->
                <div class="flex items-center">
                  <div class="rounded-xl bg-white/5 p-2 mr-3">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5 text-white">
                      <path d="M12.378 1.602a.75.75 0 0 0-.756 0L3 6.632l9 5.25 9-5.25-8.622-5.03ZM21.75 7.93l-9 5.25v9l8.628-5.032a.75.75 0 0 0 .372-.648V7.93ZM11.25 22.18v-9l-9-5.25v8.57a.75.75 0 0 0 .372.648l8.628 5.033Z" />
                    </svg>
                  </div>
                  <a href="../instance/<%= instance.Id %>" class="font-medium text-white hover:text-neutral-300 transition-colors"><%= instance.Name %></a>
                </div>
                
                <!-- Status -->
                <div>
                  <span data-status-bg class="inline-flex items-center bg-neutral-600/20 text-neutral-400 px-2.5 py-1 rounded-full text-xs font-medium">
                    <span data-status>
                      <% if (instance.InternalState) { %>
                        <%= instance.InternalState %>
                      <% } else { %>
                        <svg class="animate-spin h-3 w-3 text-white mr-1 inline-flex" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                        </svg>
                        <%= req.translations.loading %>
                      <% } %>
                    </span>
                  </span>
                </div>
                
                <!-- RAM -->
                <div class="text-sm text-neutral-400">
                  <span data-ram-usage>
                    <svg class="animate-spin h-3 w-3 text-white mr-1 inline-flex" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                      <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                      <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                    </svg>
                    <%= req.translations.loading %>
                  </span>
                  <div class="h-1.5 bg-white/10 rounded-full mt-1 overflow-hidden">
                    <div data-ram-bar class="h-full bg-emerald-500 transition-all duration-300" style="width: 0%"></div>
                  </div>
                </div>
                
                <!-- CPU -->
                <div class="text-sm text-neutral-400">
                  <span data-cpu-usage>
                    <svg class="animate-spin h-3 w-3 text-white mr-1 inline-flex" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                      <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                      <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                    </svg>
                    <%= req.translations.loading %>
                  </span>
                  <div class="h-1.5 bg-white/10 rounded-full mt-1 overflow-hidden">
                    <div data-cpu-bar class="h-full bg-emerald-500 transition-all duration-300" style="width: 0%"></div>
                  </div>
                </div>

                <!-- Disk -->
                <div class="text-sm text-neutral-400">
                  <span data-disk-usage>
                    <svg class="animate-spin h-3 w-3 text-white mr-1 inline-flex" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                      <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                      <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                    </svg>
                    <%= req.translations.loading %>
                  </span>
                  <div class="h-1.5 bg-white/10 rounded-full mt-1 overflow-hidden">
                    <div data-disk-bar class="h-full bg-emerald-500 transition-all duration-300" style="width: 0%"></div>
                  </div>
                </div>
              </div>
            </div>
          <% }); %>
        </div>
      </div>
    <% } %>
  </div>
</main>
<%- include('components/footer') %>

<style>
  /* Toggle Switch Styles */
  #instance-toggle:checked ~ .toggle-track {
    background-color: rgba(255, 255, 255, 0.2);
  }
  #instance-toggle:checked ~ .toggle-dot {
    transform: translateX(100%);
  }

  /* Mobile Optimizations */
  @media (max-width: 768px) {
    .instance-card {
      animation: slideIn 0.3s ease-out;
    }
    
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  }

  /* Touch-friendly improvements */
  @media (hover: none) and (pointer: coarse) {
    .instance-card:active {
      transform: scale(0.98);
    }
  }

  /* Safe area support for notched phones */
  @supports (padding: max(0px)) {
    .px-4 {
      padding-left: max(1rem, env(safe-area-inset-left));
      padding-right: max(1rem, env(safe-area-inset-right));
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const toggleSwitch = document.getElementById('instance-toggle');
    if (toggleSwitch) {
      toggleSwitch.addEventListener('change', function () {
        if (this.checked) {
          window.location.href = '/instances?see=other';
        } else {
          window.location.href = '/instances';
        }
      });
    }

    const instances = document.querySelectorAll('[data-instance-id]');
    instances.forEach(instance => {
      const containerId = instance.dataset.instanceId;
      const isSecure = window.location.protocol === 'https:';
      const wsProtocol = isSecure ? 'wss' : 'ws';
      const wsPort = isSecure ? '' : `:<%= config.port %>`;
      const socket = new WebSocket(`${wsProtocol}://<%= config.domain %>${wsPort}/stats/${containerId}`);

      // Query ALL elements (mobile + desktop) so both views update
      const statusElements = instance.querySelectorAll('[data-status]');
      const statusBgElements = instance.querySelectorAll('[data-status-bg]');
      const ramUsageElements = instance.querySelectorAll('[data-ram-usage]');
      const cpuUsageElements = instance.querySelectorAll('[data-cpu-usage]');
      const diskUsageElements = instance.querySelectorAll('[data-disk-usage]');
      const ramBarElements = instance.querySelectorAll('[data-ram-bar]');
      const cpuBarElements = instance.querySelectorAll('[data-cpu-bar]');
      const diskBarElements = instance.querySelectorAll('[data-disk-bar]');

      function formatBytes(bytes, decimals = 2) {
        if (bytes === 0) return '0.00 B';
        const k = 1024;
        const dm = decimals < 0 ? 0 : decimals;
        const sizes = ['B', 'MB', 'GB', 'TB', 'PB', 'EB', 'ZB', 'YB'];
        const i = Math.floor(Math.log(bytes) / Math.log(k));
        return parseFloat((bytes / Math.pow(k, i)).toFixed(dm)) + ' ' + sizes[i];
      }

      function updateAll(elements, value) {
        elements.forEach(el => el.textContent = value);
      }

      function updateProgress(bars, percent) {
        bars.forEach(bar => {
          bar.style.width = `${Math.max(0, Math.min(100, percent))}%`;
        });
      }

      // === IMMEDIATE FALLBACK FOR STOPPED CONTAINERS (this fixes the "Loading..." stuck) ===
      const currentStatusText = statusElements[0] ? statusElements[0].textContent.trim() : '';
      if (currentStatusText === 'STOPPED' || currentStatusText.includes('Offline') || currentStatusText.includes('Stopped')) {
        updateAll(ramUsageElements, `0 B <%= req.translations.ramUsage %>`);
        updateAll(cpuUsageElements, `0% <%= req.translations.cpuUsage %>`);
        updateAll(diskUsageElements, `0 B <%= req.translations.diskUsage || 'Disk Usage' %>`);
        updateProgress(ramBarElements, 0);
        updateProgress(cpuBarElements, 0);
        updateProgress(diskBarElements, 0);
        console.log('🚀 STOPPED container detected - instantly set 0 values (no WS wait needed)');
      }

      socket.onmessage = event => {
        console.log('✅ Raw WS data received for', containerId, ':', event.data);

        let stats = null;

        if (typeof event.data === 'string') {
          if (event.data.includes('The KS Wings instance appears to be down. Retrying...')) {
            statusBgElements.forEach(bg => bg.classList.add('bg-yellow-600/10'));
            statusElements.forEach(el => { el.className = 'text-orange-500'; el.textContent = 'KS Wings is Offline!'; });
            updateAll(ramUsageElements, `0 B <%= req.translations.ramUsage %>`);
            updateAll(cpuUsageElements, `0% <%= req.translations.cpuUsage %>`);
            updateAll(diskUsageElements, `0 B <%= req.translations.diskUsage || 'Disk Usage' %>`);
            updateProgress(ramBarElements, 0);
            updateProgress(cpuBarElements, 0);
            updateProgress(diskBarElements, 0);
            socket.close();
            return;
          }
          if (event.data.includes('Container not found')) {
            statusBgElements.forEach(bg => bg.classList.add('bg-yellow-600/10'));
            statusElements.forEach(el => { el.className = 'text-orange-500'; el.textContent = 'Container not found!'; });
            updateAll(ramUsageElements, `0 B <%= req.translations.ramUsage %>`);
            updateAll(cpuUsageElements, `0% <%= req.translations.cpuUsage %>`);
            updateAll(diskUsageElements, `0 B <%= req.translations.diskUsage || 'Disk Usage' %>`);
            updateProgress(ramBarElements, 0);
            updateProgress(cpuBarElements, 0);
            updateProgress(diskBarElements, 0);
            socket.close();
            return;
          }
        }

        try {
          if (event.data && event.data.trim()) stats = JSON.parse(event.data);
        } catch (e) {
          console.error('JSON parse failed for', containerId);
          return;
        }

        let realStats = stats;
        if (stats && stats.event === 'stats' && Array.isArray(stats.args) && stats.args.length > 0) {
          realStats = stats.args[0];
          console.log('✅ Unwrapped real stats for', containerId);
        }

        if (!realStats || realStats.error) {
          console.warn('Error or no stats for', containerId, realStats);
          updateAll(ramUsageElements, `0 B <%= req.translations.ramUsage %>`);
          updateAll(cpuUsageElements, `0% <%= req.translations.cpuUsage %>`);
          updateAll(diskUsageElements, `0 B <%= req.translations.diskUsage || 'Disk Usage' %>`);
          updateProgress(ramBarElements, 0);
          updateProgress(cpuBarElements, 0);
          updateProgress(diskBarElements, 0);
          return;
        }

        // RAM
        const ramUsageRaw = realStats.memory_stats?.usage || 0;
        const ramLimitRaw = realStats.memory_stats?.limit || 1;
        const ramUsage = formatBytes(ramUsageRaw);
        const ramPercent = ramLimitRaw > 0 ? Math.min(100, (ramUsageRaw / ramLimitRaw) * 100) : 0;
        updateAll(ramUsageElements, `${ramUsage} <%= req.translations.ramUsage %>`);
        updateProgress(ramBarElements, ramPercent);

        // CPU
        const cpuDelta = (realStats.cpu_stats?.cpu_usage?.total_usage || 0) - (realStats.precpu_stats?.cpu_usage?.total_usage || 0);
        const systemCpuDelta = (realStats.cpu_stats?.system_cpu_usage || 0) - (realStats.precpu_stats?.system_cpu_usage || 0);
        const cpuPercent = (cpuDelta && systemCpuDelta) ? ((cpuDelta / systemCpuDelta) * 100) : 0;
        updateAll(cpuUsageElements, `${cpuPercent.toFixed(2)}% <%= req.translations.cpuUsage %>`);
        updateProgress(cpuBarElements, cpuPercent);

        // Disk (volumeSize from your backend is in MiB)
        const diskUsageRaw = realStats.volumeSize ? parseFloat(realStats.volumeSize) * 1024 * 1024 : 0;
        const diskLimitRaw = realStats.diskLimit ? realStats.diskLimit * 1024 * 1024 : (diskUsageRaw + 1);
        const diskUsage = formatBytes(diskUsageRaw);
        const diskPercent = diskLimitRaw > 0 ? Math.min(100, (diskUsageRaw / diskLimitRaw) * 100) : 0;
        updateAll(diskUsageElements, `${diskUsage} <%= req.translations.diskUsage || 'Disk Usage' %>`);
        updateProgress(diskBarElements, diskPercent);

        // Status
        const isOffline = isNaN(ramUsageRaw) || ramUsageRaw === 0;
        statusBgElements.forEach(bg => {
          bg.classList.remove('bg-neutral-600/20', 'bg-emerald-600/10', 'bg-red-700/30', 'bg-yellow-600/10');
          bg.classList.add(isOffline ? 'bg-red-700/30' : 'bg-emerald-600/10');
        });
        statusElements.forEach(el => {
          el.className = isOffline ? 'text-red-500' : 'text-emerald-500';
          el.textContent = isOffline ? 'Offline' : 'Online';
        });
      };

      socket.onerror = () => console.error(`❌ WS error for ${containerId}`);
      socket.onclose = () => console.warn(`⚠️ WS closed for ${containerId}`);
    });
  });
</script>
