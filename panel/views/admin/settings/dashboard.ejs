<%- include('../../components/template') %>
<main id="content" class="min-h-screen p-4 md:p-8">
   <div class="max-w-7xl mx-auto">
      <header class="mb-10">
         <h1 class="text-4xl font-black text-white tracking-tight">Dashboard Settings</h1>
         <p class="text-neutral-400 mt-2 font-medium">Configure global resource limits and default settings.</p>
      </header>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
         <div class="lg:col-span-2 space-y-6">
            <div class="backdrop-blur-xl bg-white/5 border border-white/10 rounded-3xl p-8 shadow-2xl">
               <h2 class="text-xl font-bold text-white mb-6 flex items-center gap-3">
                  <svg class="w-6 h-6 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                     <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"></path>
                  </svg>
                  System Configuration
               </h2>

               <form action="/admin/settings/dashboard/update" method="POST" class="space-y-6">
                  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                     <div class="space-y-2">
                        <label class="text-[10px] font-black text-neutral-500 uppercase tracking-widest ml-1">Default Instance Slots</label>
                        <input type="number" name="defaultSlots" value="<%= settings.defaultSlots || 3 %>" class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-blue-500">
                     </div>
                  </div>

                  <div class="pt-6 border-t border-white/5">
                     <h3 class="text-sm font-bold text-white/60 mb-4 uppercase tracking-tighter">Default Instance Resources</h3>
                     <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div class="space-y-2">
                           <label class="text-[9px] font-bold text-neutral-500 uppercase">RAM (MB)</label>
                           <input type="number" name="defaultRam" value="<%= settings.defaultRam || 1024 %>" class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-2 text-white text-sm outline-none focus:border-blue-500">
                        </div>
                        <div class="space-y-2">
                           <label class="text-[9px] font-bold text-neutral-500 uppercase">CPU (%)</label>
                           <input type="number" name="defaultCpu" value="<%= settings.defaultCpu || 100 %>" class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-2 text-white text-sm outline-none focus:border-blue-500">
                        </div>
                        <div class="space-y-2">
                           <label class="text-[9px] font-bold text-neutral-500 uppercase">Disk (MB)</label>
                           <input type="number" name="defaultDisk" value="<%= settings.defaultDisk || 5120 %>" class="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-2 text-white text-sm outline-none focus:border-blue-500">
                        </div>
                     </div>
                  </div>

                  <div class="flex justify-end pt-4">
                     <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-black px-8 py-3 rounded-2xl transition-all shadow-lg shadow-blue-600/20 active:scale-95">
                        SAVE CONFIGURATION
                     </button>
                  </div>
               </form>
            </div>
         </div>

         <div class="space-y-6">
            <div class="backdrop-blur-xl bg-gradient-to-br from-blue-600 to-indigo-700 rounded-3xl p-8 text-white shadow-2xl relative overflow-hidden">
               <div class="relative z-10">
                  <h3 class="text-lg font-black uppercase tracking-tighter opacity-60">System Mode</h3>
                  <p class="text-3xl font-black mt-2">PRODUCTION</p>
                  <div class="mt-8 flex items-center gap-2 text-xs font-bold bg-black/20 backdrop-blur-md rounded-xl px-4 py-2 w-fit">
                     <span class="w-2 h-2 rounded-full bg-green-400 animate-pulse"></span>
                     LIVE UPDATES ACTIVE
                  </div>
               </div>
               <div class="absolute -bottom-10 -right-10 w-40 h-40 bg-white/10 rounded-full blur-3xl"></div>
            </div>
         </div>
      </div>
   </div>
</main>
