const EventEmitter = require('eventemitter3');

const events = new EventEmitter();

// Example events: Add more as needed
// events.emit('userLogin', userData);
// events.emit('serverCreate', serverData);
// etc.

module.exports = events;
