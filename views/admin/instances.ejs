<%- include('../components/template') %>
<main id="content">
  <style>
.input-error {
    border: 2px solid rgb(255, 69, 69);
}

.error-message {
    color: rgb(255, 69, 69);
    font-size: 0.9rem;
}
  </style>
   <div class="bg-transparent">
      <div class="sm:flex sm:items-center px-8 pt-4">
         <div class="sm:flex-auto">
           <h1 class="text-base font-medium leading-6 text-white"><%= req.translations.instances %></h1>
           <p class="mt-1 tracking-tight text-sm text-neutral-500"><%= req.translations.instancesText %></p>
         </div>
         <div class="mt-4 sm:ml-8 sm:mt-0 sm:flex-none">
           <button id="createButton" type="button" class="block rounded-xl <%= theme['button-color'] %> px-3 py-2 text-center text-sm font-medium shadow-lg transition focus:outline focus:outline-2 focus:outline-offset-2">
            <%= req.translations.createNewInstance %>
          </button>
         </div>
       </div>

 <% if (req.query.err == "CREATED") { %>
      <div class="mt-6 w-full">
        <div class="rounded-xl bg-emerald-800/10 px-4 py-6 mt-8 ml-8 mb-8 mr-8">
          <div class="flex">
             <div class="flex-shrink-0 ml-1.5">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-5 h-5 mt-2 text-emerald-400">
                  <path fill-rule="evenodd" d="M16.704 4.153a.75.75 0 0 1 .143 1.052l-8 10.5a.75.75 0 0 1-1.127.075l-4.5-4.5a.75.75 0 0 1 1.06-1.06l3.894 3.893 7.48-9.817a.75.75 0 0 1 1.05-.143Z" clip-rule="evenodd" />
                </svg>                
             </div>
             <div class="ml-5">
                <h3 class="text-sm font-medium text-emerald-400">Action completed!</h3>
                <div class="text-sm text-emerald-400/50">
                   <p>The instance was created successfully. Please allow a few minutes for it to finish installing and downloading additional files. </p>
                </div>
             </div>
          </div>
       </div>
      <% } %>	  

      <div style="display: block;" id="instanceTable" class="mt-6 w-full overflow-x-auto">
  <table class="min-w-full text-sm text-left text-white border-collapse">

    <!-- Table Head -->
    <thead class="border-b border-white/10 text-neutral-400">
      <tr>
        <th class="py-3 px-4 font-medium min-w-[180px]"><%= req.translations.name %></th>
        <th class="py-3 px-4 font-medium min-w-[120px]">State</th>
        <th class="py-3 px-4 font-medium min-w-[120px]"><%= req.translations.node %></th>
        <th class="py-3 px-4 font-medium min-w-[200px]"><%= req.translations.dockerID %></th>
        <th class="py-3 px-4 font-medium min-w-[200px]"><%= req.translations.volumeANDMount %></th>
        <th class="py-3 px-4 font-medium min-w-[200px] text-right"><%= req.translations.actions %></th>
      </tr>
    </thead>

    <!-- Table Body -->
    <tbody class="divide-y divide-white/5">
      <% if (instances.length === 0) { %>
        <tr>
          <td colspan="6" class="text-center py-4">
            <p class="text-sm text-neutral-400"><%= req.translations.noInstancesText %></p>
          </td>
        </tr>
      <% } %>

      <% instances.forEach(function(instance) { %>
      <tr class="hover:bg-white/5 transition cursor-pointer" onclick="location.href='/instance/<%= instance.Id %>'">

        <!-- Name -->
        <td class="py-4 px-4 min-w-[200px]">
          <div class="flex items-center gap-3 truncate">
            <span class="font-medium text-sm truncate"><%= instance.Name %></span>
          </div>
        </td>

        <!-- State -->
        <td class="py-4 px-4 min-w-[150px]">
          <div class="font-mono text-sm text-neutral-400 truncate">
            <%= instance.InternalState %>
          </div>
        </td>

        <!-- Node -->
        <td class="py-4 px-4 min-w-[150px]">
          <div class="font-mono text-sm text-neutral-400 truncate">
            <%= instance.Node.name %>
          </div>
        </td>

        <!-- Docker ID -->
        <td class="py-4 px-4 min-w-[250px]">
          <div class="font-mono text-sm text-neutral-400 truncate">
            <%= instance.ContainerId %>
          </div>
        </td>

        <!-- Volume -->
        <td class="py-4 px-4 min-w-[200px]">
          <div class="font-mono text-sm text-neutral-400 truncate">
            /volumes/<%= instance.VolumeId %>
          </div>
        </td>

        <!-- Actions -->
        <td class="py-4 px-4 min-w-[250px] text-right">
          <div class="flex justify-end gap-2">

            <!-- Delete Button -->
            <button onclick="location.href='/admin/instance/delete/<%= instance.Id %>'; event.stopPropagation();" class="rounded-lg bg-red-600 hover:bg-red-500 px-3 py-1.5 text-xs font-medium text-white shadow transition">
              <%= req.translations.remove %>
            </button>

            <!-- Suspend / Unsuspend -->
            <% if (instance.suspended === true) { %>
              <% if (instance['suspended-flagg']) { %>
                <button onclick="openSuspendInfoModal('<%= instance['suspended-flagg'] %>'); event.stopPropagation();" class="rounded-lg bg-yellow-600/5 border border-neutral-200/5 px-3 py-1 text-xs font-medium shadow hover:bg-yellow-400/10 hover:text-white">
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-4 w-4 text-yellow-500 mx-auto">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126ZM12 15.75h.007v.008H12v-.008Z" />
                  </svg>
                </button>
              <% } %>
              <form action="/admin/instances/unsuspend/<%= instance.Id %>" method="POST">
                <button type="submit" onclick="event.stopPropagation();" class="rounded-lg <%= theme['button-color'] %> px-3 py-1.5 text-xs font-medium shadow">
                  <%= req.translations.unsuspend %>
                </button>
              </form>
            <% } else { %>
              <form action="/admin/instances/suspend/<%= instance.Id %>" method="POST">
                <button type="submit" onclick="event.stopPropagation();" class="rounded-lg <%= theme['button-color'] %> px-3 py-1.5 text-xs font-medium shadow">
                  <%= req.translations.suspend %>
                </button>
              </form>
            <% } %>

            <!-- Edit Button -->
            <button onclick="location.href='/admin/instances/<%= instance.Id %>/edit'; event.stopPropagation();" class="rounded-lg <%= theme['button-color'] %> px-3 py-1.5 text-xs font-medium shadow">
              <%= req.translations.edit %>
            </button>

          </div>
        </td>

      </tr>
      <% }); %>

    </tbody>
  </table>
</div>
      <%- include('../components/pagination', { pagination }) %>

      <div id="instanceForm" class="hidden flex gap-4 mt-6 px-8 w-full">
        <div class="p-3 bg-transparent shadow-xl border border-white/5 rounded-xl overflow-hidden sm:p-6 lg:p-10 card" style="width: 800px;">
            <form>
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="text-neutral-400 text-sm tracking-tight mb-2"><%= req.translations.name %>:</label>
                        <input id="name" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5" placeholder="My instance">
                    </div>
                    <div>
                        <label class="text-neutral-400 text-sm tracking-tight mb-2"><%= req.translations.user %>:</label>
                        <select id="user" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5">
                            <% users.forEach(function(user) { %>
                                <option value="<%= user.userId %>" class="text-neutral-800 text-sm tracking-tight mb-2"><%= user.username %></option>
                            <% }); %>
                        </select>
                    </div>
                    <div>
                      <label class="text-neutral-400 text-sm tracking-tight mb-2"><%= req.translations.image %>:</label>
                      <select id="image" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5">
                          <% images.forEach(function(image) { %>
                              <option value="<%= image.Image %>" class="text-neutral-800 text-sm tracking-tight mb-2" data-configfilepath="<%= image.ConfigFilePath %>" data-imagename="<%= image.Name %>" data-configfilecontent="<%= image.ConfigFileContent %>" data-variables="<%= JSON.stringify(image.Variables) %>"><%= image.Name %> (<%= image.Image %>)</option>
                          <% }); %>
                      </select>
                  </div>
                    <div>
                        <label class="text-neutral-400 text-sm tracking-tight mb-2"><%= req.translations.node %>:</label>
                        <select id="node" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5">
                            <% nodes.forEach(function(node) { %>
                                <option value="<%= node.id %>" class="text-neutral-800 text-sm tracking-tight mb-2"><%= node.name %></option>
                            <% }); %>
                        </select>
                    </div>
                    <div>
                        <label class="text-neutral-400 text-sm tracking-tight mb-2"><%= req.translations.memory %> (MiB):</label>
                        <input id="memory" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5" placeholder="2048">
                    </div>
                    <div>
                        <label class="text-neutral-400 text-sm tracking-tight mb-2"><%= req.translations.cpuThreads %>:</label>
                        <input id="cpu" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5" placeholder="1">
                    </div>
                    <div>
                        <label class="text-neutral-400 text-sm tracking-tight mb-2">Disk (MiB):</label>
                        <input id="disk" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5" placeholder="10240">
                    </div>

                    <!-- Allocation selection -->
                    <div>
                        <label class="text-neutral-400 text-sm tracking-tight mb-2">Select Host Port (Allocation):</label>
                        <select id="allocationId" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5" required>
                            <option value="">-- Select node first --</option>
                        </select>
                        <small id="allocStatus" class="text-neutral-500 block mt-1">Choose an available IP:Port from the selected node</small>
                    </div>

                    <div>
                        <label class="text-neutral-400 text-sm tracking-tight mb-2">Container Internal Port:</label>
                        <input id="containerPort" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5" value="25565" placeholder="e.g. 25565" required>
                        <small class="text-neutral-500 block mt-1">Port used inside the container (usually fixed per image)</small>
                    </div>

                </div>
                <div class="mt-4">
                    <button id="createNodeBtn" type="button" class="block rounded-xl <%= theme['button-color'] %> px-3 py-2 text-center text-sm font-medium shadow-lg transition focus:outline focus:outline-2 focus:outline-offset-2">
                      <%= req.translations.create %>
                    </button>
                </div>
            </form>
        </div>
        <div id="secondCard" class="p-3 bg-transparent shadow-xl border border-white/5 rounded-xl overflow-hidden sm:p-6 lg:p-10 card hidden" style="width: 800px;">
          <form id="variablesForm">
        </form>
        </div>
    </div>
</main>

  <!-- Suspend modal remains unchanged -->
  <div id="suspendInfoModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center opacity-0 pointer-events-none transition-opacity duration-300">
        <div class="bg-neutral-800 text-white border border-white/5 rounded-xl p-8 max-w-xl w-full transform scale-95 transition-transform duration-300 shadow-lg">
            <h2 class="text-2xl font-semibold mb-2 text-neutral-200">Suspend Reason</h2>
            <p class="mb-6 text-gray-700" id="modalMessage"></p>
            <div class="flex justify-end space-x-4">
                <button onclick="closeSuspendInfoModal()" class="px-5 py-2 bg-gray-200 text-gray-800 rounded-xl hover:bg-gray-300 transition">Close</button>
            </div>
        </div>
    </div>

    <!-- Suspend modal scripts unchanged -->
    <script>
      function openSuspendInfoModal(message) {
          const modal = document.getElementById('suspendInfoModal');
          const modalMessage = document.getElementById('modalMessage');
          modalMessage.innerHTML = formatMessage(message);
          modal.classList.remove('opacity-0', 'pointer-events-none');
          modal.classList.add('opacity-100', 'pointer-events-auto');
      }

      function closeSuspendInfoModal() {
          const modal = document.getElementById('suspendInfoModal');
          modal.classList.remove('opacity-100', 'pointer-events-auto');
          modal.classList.add('opacity-0', 'pointer-events-none');
      }

      function formatMessage(message) {
          return `<div class="text-sm text-gray-600"><p class="mt-1 text-neutral-300">${message}</p></div>`;
      }
    </script>

    <!-- Image change → variables form (unchanged) -->
    <script>
      document.getElementById('image').addEventListener('change', function(event) {
          var selectedOption = event.target.options[event.target.selectedIndex];
          var variables = selectedOption.getAttribute('data-variables');
          var secondCard = document.getElementById('secondCard');
          var variablesForm = document.getElementById('variablesForm');

          if (variables) {
              secondCard.classList.remove('hidden');
              variablesForm.innerHTML = '';
              variables = JSON.parse(variables);

              for (var key in variables) {
                  if (variables.hasOwnProperty(key)) {
                      var variable = variables[key];
                      var fieldHtml = '';

                      if (variable.type === 'text') {
                          fieldHtml = `
                              <div>
                                  <label class="text-neutral-400 text-sm tracking-tight mb-2">${key}:</label>
                                  <input type="text" name="${key}" value="${variable.default || ''}" class="rounded-xl focus:ring-transparent focus:border-transparent text-white text-sm mt-2 mb-4 w-full hover:bg-white/5 px-4 py-2 bg-neutral-600/20 placeholder:text-white/20 border border-white/5" ${variable.required ? 'required' : ''}>
                              </div>
                          `;
                      }

                      variablesForm.insertAdjacentHTML('beforeend', fieldHtml);
                  }
              }
          } else {
              secondCard.classList.add('hidden');
          }
      });
    </script>

    <!-- IMPROVED: Allocation loading with better feedback -->
    <script>
      const nodeSelect = document.getElementById('node');
      const allocSelect = document.getElementById('allocationId');
      const statusText = document.getElementById('allocStatus');

      if (nodeSelect && allocSelect) {
        nodeSelect.addEventListener('change', async function() {
          const nodeId = this.value.trim();
          
          allocSelect.innerHTML = '<option value="">Loading...</option>';
          statusText.textContent = 'Loading available ports...';
          statusText.className = 'text-neutral-500 block mt-1';

          if (!nodeId) {
            allocSelect.innerHTML = '<option value="">-- Select a node first --</option>';
            statusText.textContent = 'Choose an available IP:Port from the selected node';
            return;
          }

          try {
            console.log(`Fetching allocations for node: ${nodeId}`);
            const response = await fetch(`/admin/nodes/${nodeId}/available-allocations`, {
              cache: 'no-cache' // prevent caching issues
            });

            if (!response.ok) {
              throw new Error(`HTTP ${response.status}: ${response.statusText}`);
            }

            const data = await response.json();
            console.log('Allocations response:', data);

            allocSelect.innerHTML = '<option value="">-- Choose available host port --</option>';

            if (!Array.isArray(data) || data.length === 0) {
              allocSelect.innerHTML = '<option value="">No available ports on this node</option>';
              statusText.textContent = 'No free ports found. Add some in node settings.';
              statusText.className = 'text-amber-400 block mt-1';
              return;
            }

            data.forEach(alloc => {
              const option = document.createElement('option');
              option.value = alloc.id;
              option.textContent = alloc.label;
              allocSelect.appendChild(option);
            });

            statusText.textContent = `${data.length} available port${data.length !== 1 ? 's' : ''}`;
            statusText.className = 'text-emerald-400 block mt-1';
          } catch (err) {
            console.error('Failed to load allocations:', err);
            allocSelect.innerHTML = '<option value="">Error loading ports</option>';
            statusText.textContent = 'Failed to load ports. Check console.';
            statusText.className = 'text-red-400 block mt-1';
          }
        });
      }
    </script>

    <!-- Form toggle + reset (unchanged + small improvement) -->
    <script>
      document.getElementById('createButton').addEventListener('click', function() {
        var table = document.getElementById('instanceTable');
        var form = document.getElementById('instanceForm');
        if (table.style.display !== 'none') {
          table.style.display = 'none';
          form.style.display = 'flex';
        } else {
          table.style.display = 'flex';
          form.style.display = 'none';
        }

        // Reset image and trigger change
        const imageElem = document.getElementById('image');
        imageElem.selectedIndex = 0;
        imageElem.dispatchEvent(new Event('change', { bubbles: true }));

        // Reset allocation
        allocSelect.innerHTML = '<option value="">-- Select node first --</option>';
        statusText.textContent = 'Choose an available IP:Port from the selected node';
        statusText.className = 'text-neutral-500 block mt-1';
      });
    </script>

    <!-- Create instance logic (unchanged, but good to keep) -->
    <script>
document.getElementById('createNodeBtn').addEventListener('click', async function() {
    const createBtn = this;
    createBtn.textContent = 'Working on it...';
    createBtn.disabled = true;

    const name = document.getElementById('name');
    const memory = document.getElementById('memory');
    const cpu = document.getElementById('cpu');
    const disk = document.getElementById('disk');
    const allocationId = document.getElementById('allocationId');
    const containerPort = document.getElementById('containerPort');
    const imageDropdown = document.getElementById('image');
    const selectedImage = imageDropdown.options[imageDropdown.selectedIndex];
    const configFilePath = selectedImage.getAttribute('data-configfilepath');
    let configFileContent = selectedImage.getAttribute('data-configfilecontent');
    const variablesForm = document.getElementById('variablesForm');
    
    const inputs = variablesForm.querySelectorAll('div > input');
    const variables = Array.from(inputs).reduce((acc, input) => {
        if (input.name) {
            acc[input.name] = input.value;
        }
        return acc;
    }, {});

    clearValidationErrors();

    let isValid = true;

    if (name.value.includes(' ')) {
        showError(name, 'Name must not contain spaces.');
        isValid = false;
    }

    if (!isPositiveInteger(memory.value)) {
        showError(memory, 'Memory must be an integer greater than 1.');
        isValid = false;
    }

    if (!isPositiveIntegerCpu(cpu.value)) {
        showError(cpu, 'CPU must be an integer greater than 0.');
        isValid = false;
    }

    if (!isPositiveInteger(disk.value)) {
        showError(disk, 'Disk must be an integer greater than 1.');
        isValid = false;
    }

    if (!allocationId.value) {
        showError(allocationId, 'Please select an available host port.');
        isValid = false;
    }

    if (!containerPort.value.match(/^\d+$/) || parseInt(containerPort.value) < 1 || parseInt(containerPort.value) > 65535) {
        showError(containerPort, 'Container port must be a number between 1 and 65535.');
        isValid = false;
    }

    if (!isValid) {
        createBtn.textContent = 'Create';
        createBtn.disabled = false;
        return;
    }

    if (configFileContent && configFileContent.includes('{{primaryPort}}')) {
        configFileContent = configFileContent.replace('{{primaryPort}}', containerPort.value);
    }

    const nodeData = {
        name: name.value.trim(),
        image: imageDropdown.value,
        imagename: selectedImage.getAttribute('data-imagename'),
        nodeId: document.getElementById('node').value,
        user: document.getElementById('user').value,
        memory: memory.value.trim(),
        cpu: cpu.value.trim(),
        disk: disk.value.trim(),
        allocationId: allocationId.value,
        containerPort: containerPort.value.trim(),
        configFilePath: configFilePath,
        configFileContent: configFileContent,
        variables: JSON.stringify(variables)
    };

    await sendData(nodeData, createBtn);
});

function clearValidationErrors() {
    document.querySelectorAll('.input-error').forEach(el => el.classList.remove('input-error'));
    document.querySelectorAll('.error-message').forEach(el => el.remove());
}

function showError(element, message) {
    element.classList.add('input-error');
    const errorDiv = document.createElement('div');
    errorDiv.textContent = message;
    errorDiv.className = 'error-message';
    element.parentNode.insertBefore(errorDiv, element.nextSibling);
}

function isPositiveInteger(value) {
    const num = parseInt(value, 10);
    return Number.isInteger(num) && num > 1;
}

function isPositiveIntegerCpu(value) {
    const num = parseInt(value, 10);
    return Number.isInteger(num) && num > 0;
}

function serializeParams(params) {
    return Object.keys(params)
        .map(key => encodeURIComponent(key) + '=' + encodeURIComponent(params[key]))
        .join('&');
}

function sendData(nodeData, createBtn) {
    const baseUrl = '/instances/deploy';
    const queryParams = serializeParams(nodeData);
    const fullUrl = `${baseUrl}?${queryParams}`;

    fetch(fullUrl, {
        method: 'GET',
        headers: { 'Content-Type': 'application/json' }
    })
    .then(response => {
        if (!response.ok) {
            return response.text().then(text => { throw new Error(text || 'Failed to create instance'); });
        }
        return response.json();
    })
    .then(data => {
        window.location.href = '/admin/instances?err=CREATED';
    })
    .catch(error => {
        console.error('Instance creation failed:', error);
        alert('Error: ' + (error.message || 'Failed to create instance. Check console.'));
    })
    .finally(() => {
        createBtn.textContent = 'Create';
        createBtn.disabled = false;
    });
}
    </script>
<%- include('../components/footer') %>
